image: node:18

pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g hexo-cli
    - npm install
    - hexo generate
  artifacts:
    paths:
      - public
  only:
    - main
